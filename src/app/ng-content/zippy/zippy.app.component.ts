import { Component, Directive, Input, TemplateRef, ContentChild, HostBinding, HostListener } from '@angular/core';

@Component({
    selector: 'zippy-app-root',
    templateUrl: './zippy.app.component.html'
  })
export class JerryAppComponent {}